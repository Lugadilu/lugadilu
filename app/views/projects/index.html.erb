<section class="bg-white">
    <div class="section-container">
        <div class="space-y-6">
            <h1 class="text-2xl font-bold xl:text-4xl ">My Projects</h1>
            <div class="grid xl:grid-cols-3 gap-9">

                <% @projects.each do |project| %>
                    <%= link_to project_path(project), class: "block" do %>
                        <div>
                            <%= image_tag project.image_link, class: "h-56 xl:h-96 w-full object-cover" %>
                            <h1 class="text-xl font-bold xl:text-2xl space-y-9"><%= project.title %></h1>
                        </div>
                    <%end%>
                <%end%>
            
            </div>
        </div> 
    </div>
</section>